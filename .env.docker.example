# Private Journal - Docker Environment Configuration
# For use with: docker compose --env-file .env.docker up -d

# ============================================
# REQUIRED: Encryption Key
# ============================================
# Generate with: openssl rand -base64 32
ENCRYPTION_KEY=

# ============================================
# Optional Settings
# ============================================
NODE_ENV=production
HOST_PORT=9090
DB_PATH=./data
TZ=UTC
